[pytest]
filterwarnings =
    # Suppress TFP deprecation about distutils version checks everywhere
    ignore:.*distutils Version classes are deprecated.*:DeprecationWarning
    # Blanket-ignore DeprecationWarnings originating from TFP init if any slip through
    ignore::DeprecationWarning:tensorflow_probability\.python\..*
